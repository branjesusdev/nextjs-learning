
import './spinner.css'

export const SpinnerUI = ({ className, width, height }: { className?: string, width?: number, height?: number }) => {

    return (
        <>
            <div className="ping"></div>
        </>
    )

}